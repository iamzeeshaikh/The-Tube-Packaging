---
import { region } from '../lib/chrome.js';
import { offcanvasResources } from '../lib/resources.js';
const { page } = Astro.props;
---
<Fragment set:html={offcanvasResources(region('offcanvas', page))} />
